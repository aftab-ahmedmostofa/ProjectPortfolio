# Optional: Azure OpenAI for executive insight generation.
# When unset, the app uses the built-in offline heuristic engine, so no key is
# required to run the system.
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT=
